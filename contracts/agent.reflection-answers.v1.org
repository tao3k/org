:PROPERTIES:
:CONTRACT_ORG: [[./basic.template.v1.org][basic.template.v1]]
:END:

* agent-reflection-answers-v1
:PROPERTIES:
:CONTRACT_ID: agent.reflection-answers.v1
:CONTRACT_KIND: org-elements
:CONTRACT_SCOPE: subtree
:END:

** What This Produces
This contract validates a reflection question table whose answers are not
empty. The structural contract owns the table/value flow: the model still writes
the answer, but the provider must expose table cells so the contract can reject
missing values.

The expected shape is a =Reflection Questions= or =Closure Questions= subtree
with a two-column Org table:
- =Question=
- =Value=

At least one non-header =Value= cell must be non-empty.

** reflection-has-question-table
:PROPERTIES:
:ASSERT_ID: reflection-has-question-table
:SEVERITY: error
:END:
#+BEGIN_SRC org-contract
(assert exists
  (table :descendant-of $scope))
#+END_SRC
#+BEGIN_SRC jinja2 :name message
Reflection answer receipts must include an Org table.
#+END_SRC

** reflection-has-question-column
:PROPERTIES:
:ASSERT_ID: reflection-has-question-column
:SEVERITY: error
:END:
#+BEGIN_SRC org-contract
(assert exists
  (table-cell :descendant-of $scope :text "Question"))
#+END_SRC
#+BEGIN_SRC jinja2 :name message
Reflection answer tables must include a Question column.
#+END_SRC

** reflection-has-value-column
:PROPERTIES:
:ASSERT_ID: reflection-has-value-column
:SEVERITY: error
:END:
#+BEGIN_SRC org-contract
(assert exists
  (table-cell :descendant-of $scope :text "Value"))
#+END_SRC
#+BEGIN_SRC jinja2 :name message
Reflection answer tables must include a Value column.
#+END_SRC

** reflection-has-nonempty-answer
:PROPERTIES:
:ASSERT_ID: reflection-has-nonempty-answer
:SEVERITY: error
:END:
#+BEGIN_SRC org-contract
(assert exists
  (table-cell :descendant-of $scope :column "Value" :header nil :nonempty t))
#+END_SRC
#+BEGIN_SRC jinja2 :name message
Reflection answer tables must contain at least one non-header Value cell with an answer.
#+END_SRC
