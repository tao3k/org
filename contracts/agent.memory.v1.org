#+TITLE: Contract agent.memory.v1
#+PROPERTY: CONTRACT_LIBRARY marlin.agent.contracts

* agent-memory-v1
:PROPERTIES:
:CONTRACT_ID: agent.memory.v1
:CONTRACT_SCOPE: subtree
:CONTRACT_KIND: org-elements
:END:

** What This Produces

This contract validates an agent-facing memory candidate, not a long-term
memory algorithm record.

The intended output is a recallable Org record with:

- a stable identity and memory kind;
- a source reference back to durable workspace evidence;
- explicit recall query terms and salience hints;
- contract-indexable frontier keys for hybrid recall;
- a short claim that says what should be remembered;
- source, evidence, trust, and retention sections for downstream memory
  backends and graph rerank.

=RECALL_QUERY= and the =Evidence= section are not prompt text dumps. They are the
contracted inputs that let =fd=, =rg=, and Org element query find a narrow
candidate frontier quickly, while the graph layer applies project, session,
content, evidence, trust, retention, backlink, salience, and validation facts to
return only a compact context pack.

See =org/contracts/examples/agent.memory.v1.example.org= for the concrete
memory candidate shape this contract accepts.

** memory-has-id-property
:PROPERTIES:
:ASSERT_ID: memory.has-id-property
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(let ((memory
       (headline :at $scope)))
  (assert count >= 1
    (node-property :descendant-of memory :summary (key "ID"))))
#+END_SRC

** memory-has-kind-property
:PROPERTIES:
:ASSERT_ID: memory.has-kind-property
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(let ((memory
       (headline :at $scope)))
  (assert count >= 1
    (node-property :descendant-of memory :summary (key "MEMORY_KIND"))))
#+END_SRC

** memory-has-source-ref-property
:PROPERTIES:
:ASSERT_ID: memory.has-source-ref-property
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(let ((memory
       (headline :at $scope)))
  (assert count >= 1
    (node-property :descendant-of memory :summary (key "SOURCE_REF"))))
#+END_SRC

** memory-has-recall-query-property
:PROPERTIES:
:ASSERT_ID: memory.has-recall-query-property
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(let ((memory
       (headline :at $scope)))
  (assert count >= 1
    (node-property :descendant-of memory :summary (key "RECALL_QUERY"))))
#+END_SRC

** memory-has-salience-property
:PROPERTIES:
:ASSERT_ID: memory.has-salience-property
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(let ((memory
       (headline :at $scope)))
  (assert count >= 1
    (node-property :descendant-of memory :summary (key "SALIENCE"))))
#+END_SRC

** memory-has-claim
:PROPERTIES:
:ASSERT_ID: memory.has-claim
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert count >= 1
  (headline :child-of $scope :summary (title "Claim")))
#+END_SRC

** memory-has-source
:PROPERTIES:
:ASSERT_ID: memory.has-source
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert count >= 1
  (headline :child-of $scope :summary (title "Source")))
#+END_SRC

** memory-has-recall
:PROPERTIES:
:ASSERT_ID: memory.has-recall
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert count >= 1
  (headline :child-of $scope :summary (title "Recall")))
#+END_SRC

** memory-recall-has-trigger
:PROPERTIES:
:ASSERT_ID: memory.recall-has-trigger
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(let ((recall
       (headline :child-of $scope :summary (title "Recall"))))
  (assert count >= 1
    (item :descendant-of recall)))
#+END_SRC

** memory-has-evidence
:PROPERTIES:
:ASSERT_ID: memory.has-evidence
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert count >= 1
  (headline :child-of $scope :summary (title "Evidence")))
#+END_SRC

** memory-evidence-has-entry
:PROPERTIES:
:ASSERT_ID: memory.evidence-has-entry
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(let ((evidence
       (headline :child-of $scope :summary (title "Evidence"))))
  (assert count >= 1
    (item :descendant-of evidence)))
#+END_SRC

** memory-has-trust
:PROPERTIES:
:ASSERT_ID: memory.has-trust
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert count >= 1
  (headline :child-of $scope :summary (title "Trust")))
#+END_SRC

** memory-has-retention
:PROPERTIES:
:ASSERT_ID: memory.has-retention
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(assert count >= 1
  (headline :child-of $scope :summary (title "Retention")))
#+END_SRC

** memory-retention-has-policy
:PROPERTIES:
:ASSERT_ID: memory.retention-has-policy
:SEVERITY: error
:END:

#+BEGIN_SRC org-contract
(let ((retention
       (headline :child-of $scope :summary (title "Retention"))))
  (assert count >= 1
    (item :descendant-of retention)))
#+END_SRC
